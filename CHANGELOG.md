# 变更记录（CHANGELOG）

> 本文件自 2026-08-14 起按 `docs/RELEASE.md` 维护。
> 历史部分（v0.x 阶段）从工作日志（`.workbuddy/memory/`）**重建**，commit SHA 与 Vercel 部署 ID 来自日志记录，未经 GitHub 逐条核对；「sha 未记录」的条目待补齐。
> 格式约定：版本 + 日期 + 变更类型（新增/修复/变更/移除/安全）。

---

## [1.1.0] - 2026-08-16（待发布）

### 新增
- **多清单**：清单切换 / 新建 / 重命名 / 复制 / 删除（`ListManager`）；旧单清单数据自动迁移为「默认清单」。
- 数据导入/导出新增「全部清单备份」模式（换机迁移）；导入自动识别条目数组与备份两种格式。
- 手机端底部固定操作栏（添加条目 / 导出截图 / 更多），次要操作收纳进「更多」抽屉。
- 统一确认对话框（替代原生 `confirm`，删除/清空/覆盖导入/删除清单均有确认）与顶部 Toast 提示。
- 保存失败提示：浏览器存储写入失败时提示用户立即导出备份（修复 P0-2）。

### 变更
- **手机端界面全面重设计**：吸顶清单头部（清单名 + 条数）；条目卡片信息层级重构（品名标题 + SPD 副行 + 规格/备注字段块，整块可点编辑）；弹窗统一改为底部抽屉（`Sheet`，键盘友好、安全区留白）；空状态引导修正；取消禁止页面缩放。
- 桌面端轻量对齐：头部改为清单切换 + 计数；工具栏样式统一；删除确认统一为对话框。
- 排序弹窗显示品名 / 规格 / 备注（原来只显示备注，空备注条目无法辨认）。
- 截图导出文件名含清单名（区分周六/周日/周一清单）。
- 导出截图抽屉在手机端也显示完整 A4 表格预览（首版重设计曾误改为摘要卡片，用户反馈后恢复）。
- 移除卡片字段旁的铅笔小图标（用户反馈「不需要这些小图标」，整块点击编辑不变）。
- 品名编辑改为多行文本框（长品名显示 2 行时编辑不再折成 1 行），Enter 保存、Shift+Enter 换行。
- 编辑态与展示态同框同尺寸：SPD 编辑时标签保持显示、规格/备注保持原块样式、编辑边框改为细 ring（避免像报错红框）。
- 卡片「编辑/删除」按钮从头右移到卡片底部左侧一行（避免与右下角「供货目录」悬浮按钮重叠，也解决长品名时右上角留白失衡）。
- 手机端「供货目录」悬浮按钮改为 56px 圆形图标按钮并上移（减少对内容的遮挡，桌面端保持带文字胶囊）。
- **行内编辑态彻底去边框化**：编辑框无高亮边框/无背景/无内边距（`.inline-input`），与展示文本宽高完全一致（实测差 0px），仅光标提示编辑态。
- **品名/SPD 改为「下划线表单线」**：展示态即带淡色下划线（可编辑暗示），编辑态仅下划线变为主题色 + 光标，几何尺寸零变化；同时恢复 `maximumScale: 1` 防止 iOS 聚焦小字号输入框自动缩放页面导致跳动；占位符颜色与展示态一致。
- **SPD 预设快捷选择（多选）**：SPD 编辑态与添加表单均显示预设胶囊，**支持多选**——点击追加、再点取消，选中值以「，」合并显示（如「中盛，和佳」）；内置预设为供货目录的 11 家公司名（中盛/和佳/上海葆馥/恒匡/瑞兴/康铭/洲淮/融智/元康/春晖/美悦）；「预设管理」新增「预设 SPD」区（增删快捷项）。
- **PWA 更新改为用户确认式**（修复断网打开出错）：SW 安装**不再自动激活**（去掉 install 里的 skipWaiting）——缓存优先打开 + 后台静默检查；新版本就绪后弹横幅「立即更新 / 稍后」；确认才激活新版并自动刷新；取消则旧版完整运行（旧缓存不清理）；新增「设置」抽屉（版本与更新状态、「确认更新」按钮、「检查更新」按钮、预设管理入口、数据说明）；`lib/sw-client.ts` 统一管理更新状态（事件 + 2 秒轮询兜底时序竞态）；SW 缓存 v4 → v5。
- **卡片紧凑化**：标签与内容同行、压缩各块高度与间距（品名 30px / SPD 24px / 规格 34px / 操作行 28px），平均卡片高度 ~205px，一屏可完整显示 3 张以上。
- **备注结构化**：备注拆分为「病人名字 / 住院号 / 预设科室 / 预设主治医生」四字段；科室与医生支持预设快捷选择（点击胶囊填入/再点取消）；卡片上备注按两行两列网格排版；导出/备份文本仍为「名字，住院号，科室，医生」；旧自由文本备注兼容显示。
- **预设管理**（新增入口：手机「更多」抽屉 + 桌面工具栏）：增删科室与医生快捷选项，内置科室「骨一/骨二」、医生 10 人名单；预设独立存储于 `medical-supplies-table.presets.v1`。
- 版本 1.0.0 → 1.1.0；SW 缓存 v3 → v4。

### 验证
- 本地 `tsc --noEmit` 与 `next build` 通过（6 静态页零错误）；无头浏览器 51 项布局体检全过（编辑态宽高差 0px、展示/编辑下划线一致、SPD 11 家预设与多选、卡片紧凑、备注结构化全套、设置抽屉、sw.js 无自动激活），并经视觉复核确认。
- **PWA 更新流程端到端模拟 7/7 通过**：首次加载 SW 接管 → 模拟发布新版 → 新 SW 进入 waiting 不自动激活 → 旧版完整运行（页面未刷新、controller 未变）→ 横幅「立即更新/稍后」→ 稍后消失 → 设置显示「新版本已就绪」+「确认更新」→ 确认后自动刷新并运行新版。

### 文档
- 建立项目管理文档体系：`docs/`（审查报告、管理机制、架构、发布流程、供货目录指南）、`CONTRIBUTING.md`、`SECURITY.md`、`.github/`（CI、CODEOWNERS、PR/Issue 模板）。
- 修正 `README.md` 过时内容（html2canvas 已移除、品名/规格可留空、功能清单更新）。

---

## v0.1.0 及更早（2026-08-12 ~ 2026-08-14，历史重建）

### 2026-08-14 — 五项改进（sha 未记录）
- **新增**：数据导入/导出（`components/DataTransfer.tsx`，JSON 复制导出 + 粘贴导入，含校验与覆盖确认）。
- **新增**：拖拽排序（`components/ReorderModal.tsx`，HTML5 拖拽 + ▲▼ 微调，保存后重排序号）。
- **新增**：`lib/version.ts`（APP_VERSION 页脚展示）；SW 更新检测 + 底部「新版本可用」横幅（`sw-register.tsx`）。
- **变更**：手机卡片隐藏「请购数/单位」只读字段，收紧间距；桌面表格与两处导出列宽同步压缩（请购数 66→56、单位 48→40）。
- **变更**：SW 导航策略改为缓存优先（stale-while-revalidate），缓存版本 v2→v3。

### 2026-08-13
- **新增**（`0abd928`）：SPD 列一键复制（格式「SPD 器械申请」+ 逐行「序号 <SPD>」，含剪贴板降级）；手机卡片品名多行编辑。
- **变更**（`cc5cca9`）：导出适配 A4（宽 794px）；全部文字改纯黑（去掉 SPD 蓝色）。
- **变更**（`924b4bdf`）：导出列宽微调（规格/备注收窄，品名加宽；备注加粗）。

### 2026-08-12（16 次迭代）
- **新增**（`da14aec4`）：添加条目弹窗内可打开供货目录参考（面板层级提升至弹窗之上）。
- **新增**（`48d8aaa`）：供应商/品牌目录悬浮折叠面板（11 家供应商）；**修复** PWA 图标损坏——Git Data API 推送改为 Buffer+base64（此前 UTF-8 文本读取破坏 PNG 魔数）；SW 缓存 v1→v2。
- **新增**（`3ed77d47`）：离线 Service Worker（`public/sw.js` + `app/sw-register.tsx`，生产环境注册）。
- **新增**（`56533dd`）：PWA 图标与 manifest，支持安装到主屏（`app/manifest.ts`、`public/icons/`、`apple-touch-icon`）。
- **变更**（`c512b138`）：手机端表格改为卡片布局（响应式：<640px 卡片，桌面表格不变）。
- **变更**（`f95adc4`）：**弃用 html2canvas**，导出改用原生 Canvas 2D 绘制（新增 `lib/drawExport.ts`），彻底解决「预览居中、保存不居中」；移除 html2canvas 依赖。
- **变更**（`62eb26b`）：品名/规格取消必填（允许留空）；移除「暂存/载入暂存」功能（理解偏差，回滚）。
- **修复**（`8a52b7a`）：手机端中文衬线字体——Google Fonts 改用中国镜像 `fonts.googleapis.cn`，body 字体栈加中文衬线兜底。
- **修复**（`1448021`）：导出中文非衬线问题——显式加载含 CJK 字形的 Noto Serif SC。
- **变更**（`523a5d7`）：导出仅保留表格主体；单元格垂直居中；新增「暂存草稿」功能（后被移除）。
- **修复**（`745d123`）：导出截图字体大小错乱（html2canvas 对 transform 缩放祖先的 bug）——改用离屏原尺寸克隆 + 字体预加载。
- **变更**（`3b72354` / `bad90ebe`）：GitHub 仓库初始推送 + README 部署说明；完成 GitHub → Vercel 自动部署链路（推 main 即上线）。
- **变更**（`5d52e6e`）：手机端从卡片回退为横向表格（用户要求）。
- **变更**（`23c7f5b`）：手机优先卡片化 + 截图 2x→4x（后被回退）。
- **新增**（首版，2026-08-12 上线）：本地存储的医用耗材录入表——就地编辑、添加/删除、序号自动重排、html2canvas 截图导出；部署到 Vercel（`https://medical-supplies-table.vercel.app`）。
