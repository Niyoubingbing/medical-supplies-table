# 安全说明

## 1. 数据边界（对用户）

- 本应用**纯本地**运行：所有条目数据仅存于用户浏览器的 `localStorage`，不上传服务器、无账号体系、无埋点。
- 服务器（Vercel）只托管静态代码文件，不接收业务数据。
- 风险提示：清除浏览器数据 / 卸载应用 / 更换设备会导致数据丢失——请用「数据导入/导出」定期备份 JSON。

## 2. 对维护者的安全要求

### 2.1 凭据管理

- GitHub PAT：只作环境变量使用；最小权限（`repo`）；有效期 ≤ 90 天（建议 30 天）；设备丢失立即撤销。
- **禁止**：把 PAT 写入日志、文档、commit、issue、聊天记录。工作日志中引用一律打码（`ghp_xxxx`）。
- 部署优先使用 Vercel 官方 GitHub App 集成（已配置的 push 自动部署不需要 PAT）；备用脚本 `push-to-github.mjs` 需要 PAT 时按上两条执行。

### 2.2 已知事件与处置（2026-08-12 工作日志泄露 PAT）

- 事件：完整 PAT 曾明文写入 `.workbuddy/memory/2026-08-12.md`（OneDrive 同步目录内）。
- 处置：**立即在 GitHub → Settings → Developer settings → Personal access tokens 撤销该 token**；后续日志打码。
- 教训：凭据不进任何文件；memory 日志发布前检查敏感信息。

### 2.3 依赖与供应链

- 提交 `package-lock.json`（锁定版本），CI 用 `npm ci` 安装。
- 维护者月度例行 `npm audit`，高危项立即处理（当前依赖极少，风险低）。

### 2.4 仓库可见性

- 当前仓库公开，但内含供应商-品牌业务信息。是否改私有由所有者决策（`docs/REVIEW.md` §4 决策项 D1）。
- 公开仓库需 LICENSE（当前缺失，决策项 D2）。

## 3. 报告安全问题

发现漏洞或泄露：**不要开公开 Issue**。直接联系所有者（GitHub 私信 / 内部渠道），修复后再酌情公开。

## 4. 隐私与合规提示

医疗相关场景使用前，请结合院内规定评估「数据留在个人设备」是否符合要求；本应用不提供任何合规背书。
